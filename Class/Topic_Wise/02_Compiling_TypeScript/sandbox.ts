const character = "Roman";
console.log(character);
const inputs = document.querySelectorAll("input");
// as we can see that we can write all javascript code in typescript code as well
inputs.forEach((input) => {
  console.log(input);
  console.log("hello");
});
